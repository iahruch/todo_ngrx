export interface Todo {
  id: string;
  text: string;
  completed: boolean;
}

export interface ITodo {
  id: number | string;
  email: string;
  first_name: string;
  last_name: string;
  avatar: string;
}
